# 17 - 后端 IR 提示 + 中间层优化（mcs_ir）

> 思路：**编译器只管输出「类 IR 提示」注释，值级优化交给中间层 `tools/mcs_ir.py`**
> （asm 文本级）。好处：优化迭代快，不必每次重编自举编译器。

## 提示格式

后端 `CodeGen.emitTrace` 在每条 AIR 指令前输出一行注释：

```
; v<inst> <tag> <操作数…> [-> @spx<disp>]
```

例：

```
; v2 add_wrap v0 c -> @spx-1
        add a,#0x03
        mov @spx-0x1,a
```

- `v<inst>`：AIR 指令号（值号）；操作数里的 `vK` 是值、`c` 是编译期常量。
- `-> @spx<disp>`：该值的结果帧槽（十进制位移）。
- 注释不影响 `sdas`；中间层消费后删掉。

## 中间层 `tools/mcs_ir.py`（夹在 `mcs_opt.py` 之后、`sdas` 之前）

1. **死 store 消除**：`mov @spx<d>,X` 若 `@spx<d>` 在该函数其后**再也不出现**
   （读/写都算出现），说明该帧槽是死值 → 删掉这条 store。
   **计算本身保留**：`mcs_opt` 已把值留在 A，供后续指令直接用。
2. 删掉所有 IR 提示注释。

安全：只删「本函数其后无任何 `@spx<d>` 出现」的 store；函数边界按全局标号 `_sym:` 划分；
扫描时跳过注释行（避免提示里的 `@spx<d>` 自证）。

## 与 `mcs_opt.py` 的关系

`mcs_opt.py` 需把整行注释**附到上一条**（`_parse` 里 `entries[-1].raw += line`），
否则注释会变成新条目、**打断 R1/R3/R4/R5 的跨指令模式匹配**（相邻两条 `mov` 之间会被注释隔开）。

## 实测

`examples/vscode_mixed_xmake_zig` 的 `lib.zig`（`export fn zig_calc(u8) u8`）：

| 阶段 | Zig asm 主体指令 |
| --- | --- |
| 后端原始 | ~9（含 3 条 `mov @spx…,a`） |
| `mcs_opt` 后 | ~7 |
| `mcs_ir` 后 | **5**（`mov a,dpl; add a,#3; xrl a,#0x2a; mov dpl,a` + 帧） |

QEMU 仍打印 `zig_calc(5)=22`。

## 接入

纯 Zig 目标的管线：`fix_mcs_labels → mcs_opt → mcs_ir → sdas → 链接`。
当前只在 `examples/vscode_mixed_xmake_zig/`（`xmake.lua` 与 `build.zig`）接入；
主工程 `xmake/helpers.lua` 的 `postprocess_asm` 可同样加一行，惠及所有 Zig 目标。
