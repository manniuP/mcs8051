# 待推进工作清单（2026-09-19）

> 承接 [`待推进-2026-09-18.md`](待推进-2026-09-18.md)。**重编编译器（自举）前需经用户确认；push 先问。**
> 改编译器前先看 [`27-上游迁移协定.md`](27-上游迁移协定.md)；跟上游流程见 [`26-上游跟进方案.md`](26-上游跟进方案.md)。

## 0. 本轮已完成（2026-09-19）

- **示例整理**：按平台分 `examples/ai8051u/` 与 `examples/at89c52/`（去冗余前缀）；`portable/`、`vscode_*` 留根。
- **产物集中**：构建产物统一 `build/examples/<平台>/<名>/`；`xmake/helpers.lua` 的 `outdir`；
  `.gitignore` 只忽略 `build/`；工具/CI/便携/文档路径全部同步。
- **SFR 可读 API**：`mcs_sfr.py` 生成 `dev.reg`（寄存器对象）/`dev.p`（C 风格固定地址指针）；
  `mcs_opt.py` 新增 **R6**（固定地址 RMW 融为单条）+ **R4 扩到直接地址**；新增 `sfr_bits`/`sfr_reg`/`sfr_ptr` 示例。
- **文档**：新增 `24-优化器管线总览`、`25-MCS后端框架`、`26-上游跟进方案`、`27-上游迁移协定`；
  更新 `06/09/15/23` 与索引。
- **编译器迁移 + 单仓**：`mcs251/compiler` 改为 **submodule** → `manniuP/zig-mcs51-backend`（`mcs251-backend`）；
  `mcs251` 与 `mcs8051` **合并为同一历史**（合并提交 `2e67b4e2`，内容以公开仓为准）。
- **已 push**：fork `9f79b62630`；`mcs8051` main `c2195a4300`（含本清单、docs 19/24–27、B1、修复）。
- **B1 优化等级逐级**（后端，已重编）：O2/O3/Ofast 比较融合（范围安全），O0/O1 不融合，Os 同前。
- **D1/D2**：STC32G 复位/向量核实并修设备表；`docs/19-设备表`；`dev` 示例改 `dev.reg`/`dev.p`。
- **CI**：`mcs_regress` 预算按 `-Dmcs-small=true` 实测——`ci.ps1` 更正为开启 `mcs_small`（原先就红）。
- **修既有 bug**：8 位 Zig `export var` 被设备表自动放进 DSEG，与 C `--model-large` 的 xdata extern 失配
  （simtest `failcode=0x41`）；`device.decide` 加 arch，**mcs51 默认 xdata** → simtest 恢复 `aa 00 02 04 08 10 20 40`。
  `ci.ps1` 的 sim 步改为**自动生成 `simtest.cmd`**（原先缺文件被静默跳过，bug 才没暴露）。见 `docs/19`。

## A. 迁移 / 发布收尾（用户定）

- **A1.** ✅ **已合并为同一仓库/历史**（2026-09-19）：`mcs251` = 本地工作树，`origin = manniuP/mcs8051`（`main`），
  **内容以公开仓为准**；公开仓合并提交 `2e67b4e2`（历史含两条根）。发布 = `git push origin main`，
  详见 [`发布同步策略.md`](发布同步策略.md)。
- **A2.** 原 `<workspace>/zig` clone 现冗余（fork 的旧 clone）——保留作上游镜像，或删除（删大件前确认）。
- **A3.** fork 里的 `experimental/` 快照是 delta 最大的非 `codegen/mcs` 块——保留/移出/标注，待定。
- **A4.** 接入 patch（11 文件）后续按 `27` **单独 commit**；历史不重写。
- **A5.** 上游 1.0 到来：按 `26 §2/§5` rebase（重生成 `forms.zig`、补 exhaustive switch、重编、跑 CI）。

## B. 优化余项

- **B1. 优化等级细化** ✅（2026-09-19，首步；已重编编译器）：后端加 `opt_level`（`linksection` 标签优先，
  否则 `-OReleaseSmall→Os`/`-OReleaseFast→Ofast`/其余→`O3`）。**O2/O3/Ofast 启用「比较紧跟 `cond_br`」
  融合**（范围安全：短条件反向跳过 + 远跳 `ejmp`），**O0/O1 保留 bool 物化**，`Os` 同前（融合 + 直接短跳 + 冷区）。
  O1 暂与 O0 相同；更细旋钮（短跳松弛/指令选择/常量传播）留后续。已提交/推 fork（`9f79b62630`）。
- **B2. R6 残留** ✅ 结论：**不融合（安全性）**——函数**最后一条** RMW 之后 A 仍可能是返回值
  （4 字节返回的返回寄存器 `A:B:DPH:DPL` 含 A），文本层无法判定；「保留 A」的替换版不减指令，无意义。
  见 `docs/24-优化器管线总览.md §5`。
- **B3. 寄存器提升 / 跨块值级 CSE**：需后端分配器/AIR CSE，**大工程，建议单独立项**；需重编编译器。
- **B4.** `mcs_ir` 对含 `push/pop` 的函数整体跳过（如需收益要更精细的 SPX 建模）。
- **B5.** 预算回归目前只测 `-Dmcs-small`（CI 已改）；可考虑同时记录非 small 档基线。

## C. 调试支持（平台无关方向）

- **C1. UART 调试桩**（首选）：固件内置断点/单步/内存/寄存器，走 COBS 帧；设计见
  [`设计-UART-RSP调试桩.md`](设计-UART-RSP调试桩.md)。**M0 尚未动代码**。
- **C2. Zig CDB 调试器余项**：栈展开（SPX/3 字节返回/4 字节中断帧）、局部变量、`next` 真正跨函数、
  给 Zig 数据发 `G$`（看全局变量）。
- **C4. 可视化外挂**（暂缓）：串口 + P0–P7 → 网页/示波器；适配器外挂转发进程，不动调试器本体。
- **C5. STC 真机 monitor**（敏感，本地、不入公开仓）：Keil Monitor-251 专有；只读理解/逆向前先确认授权。

## D. 设备表 / 便携余项

- **D1.** ✅ STC32G12K128 复位/向量 = FF:0000（手册 11.1）；设备表 `base/reset=0xff0000`、eeprom/中断向量
  已修，工具单段钳到地址空间顶部（128K→顶部 64K）。**全 128K（FE:0000 下半 bank）需拆分链接**——未做。
- **D2.** ✅ `docs/19-设备表` 已写；`examples/ai8051u/dev` 改用 `dev.reg`/`dev.p`。
  **其余示例推广 `dev.reg`/`dev.p`** 未做（仍 `dev.sfr` + `mcs` 宏）。
- **D3.** 便携集：构建脚本已同步（`--dep dev` + 生成 `device_sfr.zig`）；**打包 + 真机验证未做**
  （`usb_cdc` hello world；需硬件）。便携 `zig.exe` 可再瘦身（补 `.mcs` dev env）。

## E. 旧优化脚本复查（用户自查）

- `old-scripts/` 已放新旧版 + 已知问题标注；待用户找其它 bug 后决定修/提 issue。

## F. 真机未验

- `exdata`（片外）放置；`usb_cdc` 的 `hello world`；R6/dev.p 新示例真机（`sfr_ptr`）。

## G. 合规 / 论坛

- 论坛发帖（Discuz 对 Markdown 支持有限：表格改列表、代码块用引用）。
- STC/MCS 相关的 `dev.reg`/`dev.p` 若入公开仓，确认无 STC 专有内容（目前仅通用 SFR 名与地址）。

## I. EEPROM / 外部存储（兜底，待评估）

- STC IAP EEPROM 不能 XIP → 代码需搬 RAM 执行（类冷代码 overlay）；常量经 IAP 读。
- 文件系统移植（LittleFS 优先，FatFs 备选）：适配 SDCC mcs251（1 字节 IO、无标准 libc 文件接口）。
